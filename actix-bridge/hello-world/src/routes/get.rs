use actix_web::{HttpResponse, Responder, get};

// #[get("/eth")]
// async fn get_latest_block() -> impl Responder {
//     let block = provider.get_block_number().await?;
//     HttpResponse::Ok().body(block)
// }
